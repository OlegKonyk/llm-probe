# Development Environment Configuration
# Optimized for local development with Ollama

# =============================================================================
# API Key Authentication (Local Development)
# =============================================================================

# Use simple env-based auth for local development (no AWS costs)
API_KEY_PROVIDER=env

# Generate a secure API key for local testing
# Run: openssl rand -hex 32
API_KEY=dev-api-key-replace-me

# Optional: Disable API key auth entirely for local development
# API_KEY_AUTH_ENABLED=false

# =============================================================================
# LLM Provider Configuration (Local Development)
# =============================================================================

# Use Ollama for local development (free, fast, no AWS costs)
LLM_PROVIDER=ollama

# =============================================================================
# Ollama Configuration
# =============================================================================

# Local Ollama service
OLLAMA_HOST=http://localhost:11434

# Use llama3.2:latest for local testing (3.2B parameters, fast)
# Alternative: llama3.1:8b (higher quality, slower)
OLLAMA_MODEL=llama3.2:latest

# Generous timeout for local development (30 seconds)
OLLAMA_TIMEOUT_MS=30000

# =============================================================================
# Server Configuration
# =============================================================================

# Standard development port
PORT=3000

# Development environment
NODE_ENV=development

# =============================================================================
# CORS Configuration
# =============================================================================

# Allow multiple localhost ports for frontend dev servers
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# =============================================================================
# Rate Limiting
# =============================================================================

# Generous limits for local development (15 min window)
RATE_LIMIT_WINDOW_MS=900000

# High request limit for testing (100 requests per 15 min)
RATE_LIMIT_MAX_REQUESTS=100

# =============================================================================
# Development Tips
# =============================================================================
#
# 1. Start Ollama: ollama serve
# 2. Pull model: ollama pull llama3.2:latest
# 3. Start backend: npm run dev
# 4. Run tests: npm test --workspace=ts-test
#
# Ollama models for different use cases:
# - llama3.2:latest (3.2B) - Fast, good for testing
# - llama3.1:8b (8B)       - Better quality, still fast
# - mistral:7b (7B)        - Alternative, good quality
